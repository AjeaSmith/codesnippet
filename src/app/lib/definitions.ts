export type Folder = {
  id: string;
  name: string;
  icon: string;
};

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Tag = {
  name: string
}